
streamlit>=1.38.0
pandas>=2.2.0
numpy>=1.26.0
plotly>=5.24.0
python-dateutil>=2.8.2
scipy>=1.14.0
statsmodels>=0.14.0
pymc>=5.10.0 # Kept for now, but monitor if issues persist

sqlalchemy>=2.0.0 # For ORM and database interaction (works with SQLite and others)

copulas>=0.11.0 # Commented out as a likely cause of installation issues
pmdarima>=2.0.4 # Handled conditionally in ai_models.py

prophet>=1.1.5 # Commented out as a likely cause of installation issues

tensorflow>=2.17.0 # Explicitly commented out due to Python 3.13 compatibility issues on Streamlit Cloud
torch>=2.4.0 # Explicitly commented out

lifelines>=0.28.0
ruptures>=1.1.9
scikit-learn>=1.5.0
yfinance>=0.2.37 # Added for fetching benchmark data
thefuzz>=0.20.0 # For fuzzy string matching in column mapper
python-Levenshtein>=0.23.0 # Optional, but improves thefuzz performance
chardet>=5.0.0 # For CSV encoding detection

poetry>=1.7.0 # User can uncomment if they choose to use Poetry
pipenv>=2023.10.3 # User can uncomment if they choose to use Pipenv

python-dotenv>=1.0.0
